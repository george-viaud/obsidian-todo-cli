# Inbox

> Freeform capture. Add quick items here from the phone — no format rules.
> On desktop, run `todo inbox` to promote items into the structured `TODOs.md`.
>
> **Do not use `#task` here** — only structured items in `TODOs.md` get that tag.

- 

